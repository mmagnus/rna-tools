checks:
    python:
        code_rating: true
        duplicate_code: true

filter:
    excluded_paths:
        - '*/test/*'
        - '*.min.js'
    dependency_paths:		
        - 'rna_tools/tools/clarna_play/*'
        - 'rna_tools/tools/mini_moderna3/*'
build:
    environment:
        python: 3.7.4


build:
  nodes:
    analysis:
      tests:
        override:
          - py-scrutinizer-run
